syntax = "proto3";
option go_package = "github.com/fzdwx/burst/api";

service burst{
  rpc Export(ExportRequest) returns (ExportResponse);
}

message ExportRequest {
  int64 clientWsPort =1;
  repeated int64 clientPort = 2;
}

message ExportResponse {
}
